name: "Build App"
description: Builds the app
inputs:
  platform:
    description: Wails target platform
    required: true
runs:
  using: composite
  steps:
    - name: Build with Wails
      working-directory: ./src
      shell: bash
      run: wails build -platform "${{ inputs.platform }}" -webview2 embed
